#include<stdio.h>
main()
{
	char fir[50],sec[50];
	int i,c=0,a;
	printf("The ");
	/*
	printf("Enter first:");/*
	gets(fir);
	
	printf("Enter second:");
	gets(sec);/*
	
	for(i=0;fir[i];i++)
	{
		if(fir[i]==sec[i]||fir[i]-32==sec[i]||fir[i]+32==sec[i])
		continue;
		
		else
		c++;
	}
	printf("The first string %s",fir);
	printf("\nThe second string %s\n",sec);
	
	if(c==0)
	printf("The string is same");
	
	else
	printf("The string is not same");
	
	
	
	*/
	
}
